# Let git normalise text files, but never hand CRLF to the Linux box.
# The fal policy YAML silently failed to apply once because of CRLF endings.
* text=auto

*.sh text eol=lf
*.yaml text eol=lf
*.yml text eol=lf
Caddyfile text eol=lf
*.service text eol=lf
*.mjs text eol=lf
